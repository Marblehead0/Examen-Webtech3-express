
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
Geef hier in wie je bent, welk examen je wilt inhalen en de reden hiervoor.
  <ul class="quotes">
  <% for(var i=0; i<quotes.length; i++) {%>
    <li class="quote">
      <span><%= quotes[i].name %></span>
      <span><%= quotes[i].quote %></span>
    </li>
  <% } %>
  </ul>

  <form action="/quotes" method="POST">
    <input type="text" placeholder="naam" name="naam">
    <input type="text" placeholder="examen" name="examen">
    <input type="texr" placeholder="reden" name="reden">
    <button type="submit">Submit</button>
  </form>
</body>
</html>